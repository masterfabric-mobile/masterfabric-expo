/**
 * Custom stack header row — use with `SafeAreaView` (`edges` includes `top`)
 * so the back control and title align across modal/stack screens.
 */
export const STACK_HEADER_ROW = {
  paddingHorizontal: 16,
  paddingTop: 16,
  paddingBottom: 20,
} as const;
