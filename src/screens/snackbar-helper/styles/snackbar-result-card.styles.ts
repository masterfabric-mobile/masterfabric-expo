import { StyleSheet } from 'react-native';

export const snackbarResultCardStyles = StyleSheet.create({
  container: {
    borderRadius: 12,
    padding: 20,
    marginBottom: 16,
    borderWidth: 1,
  },
  header: {
    marginBottom: 16,
  },
  functionName: {
    fontSize: 17,
  },
  section: {
    marginBottom: 12,
  },
  sectionLabel: {
    fontSize: 13,
    marginBottom: 4,
  },
  sectionValue: {
    fontSize: 14,
    lineHeight: 22,
  },
});

