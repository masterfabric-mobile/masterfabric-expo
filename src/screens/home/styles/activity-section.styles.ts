import { StyleSheet } from 'react-native';

export const activitySectionStyles = StyleSheet.create({
  section: {
    marginBottom: 32,
  },
  sectionTitle: {
    fontSize: 20,
    fontWeight: '600',
    marginBottom: 16,
  },
  activityCard: {
    padding: 20,
    borderRadius: 16,
    alignItems: 'center',
  },
  activityText: {
    fontSize: 16,
    opacity: 0.7,
    textAlign: 'center',
  },
});
