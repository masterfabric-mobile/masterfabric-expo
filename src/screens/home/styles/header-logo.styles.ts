import { StyleSheet } from 'react-native';

export const headerLogoStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  logo: {
    marginRight: 12,
  },
  appName: {
    fontSize: 18,
    fontWeight: '600',
  },
});
