import { StyleSheet } from 'react-native';

export const timezonePickerStyles = StyleSheet.create({
  // Container
  container: {
    marginBottom: 16,
  },
  
  // Label
  label: {
    fontSize: 14,
    fontWeight: '600',
    marginBottom: 8,
  },
  
  // Picker container
  pickerContainer: {
    borderRadius: 12,
    overflow: 'hidden',
  },
});
