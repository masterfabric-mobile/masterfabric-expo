import { StyleSheet } from 'react-native';

export const packageInfoStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: 4,
  },
  text: {
    fontSize: 12,
    textAlign: 'center',
  },
});
