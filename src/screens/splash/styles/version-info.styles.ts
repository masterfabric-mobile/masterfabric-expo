import { StyleSheet } from 'react-native';

export const versionInfoStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: 8,
  },
  versionText: {
    fontSize: 16,
    fontWeight: '500',
  },
  nameText: {
    fontSize: 14,
    marginTop: 2,
    opacity: 0.8,
  },
});
