import { toastService as coreToastService } from 'masterfabric-expo-core';

// Re-export core toast service for backward compatibility
export const toastService = coreToastService;
export { ToastService } from 'masterfabric-expo-core';

